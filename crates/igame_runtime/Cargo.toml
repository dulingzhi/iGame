[package]
name = "igame_runtime"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy_math = { version = "0.18", default-features = false, features = ["std"] }

[dev-dependencies]
bevy_math = { version = "0.18", default-features = false, features = ["std"] }
